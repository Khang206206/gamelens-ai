# Web application

This directory is reserved for the Next.js and TypeScript application
introduced in Stage 2, after the backend catalog contract is stable.

The frontend will use typed API models, environment-based API configuration,
responsive and accessible components, and explicit loading, empty, and error
states.
