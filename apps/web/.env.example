# Browser-visible FastAPI base URL for direct npm development.
# Copy this file to .env.local before running npm commands.
# The OpenAPI generation/check scripts load this file through Next's env loader.
NEXT_PUBLIC_API_URL=http://localhost:8000

# Public identifier for the exact consent copy accepted by the API.
NEXT_PUBLIC_CONSENT_VERSION=stage-4-v1
