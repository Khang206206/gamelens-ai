# API application

This directory is reserved for the FastAPI application introduced in Stage 1.

The implementation will separate routes, schemas, services, repositories,
database models, and configuration. Its first vertical slice will include
settings, PostgreSQL connectivity, Alembic, a health endpoint, and tests.

No placeholder endpoint or fake recommendation implementation is included in
Stage 0.
